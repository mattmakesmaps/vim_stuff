vim_stuff
=========

a vimrc, some plugins, and a lot of hope.

all of these plugins are made by smart folks (not me).

# Install

    $ cd /home/user/.vim
    $ ln -s /path/to/repo/doc
    $ ln -s /path/to/repo/plugin
    $ cd /etv/vim
    $ ln -s /path/to/repo/vimrc
